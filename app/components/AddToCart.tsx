'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button className='btn bg-sky-400 text-white hover:bg-sky-500' onClick={() => console.log('button was clicked')}>Add to card</button>
    </div>
  )
}

export default AddToCart
